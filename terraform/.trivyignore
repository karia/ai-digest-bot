# AWS-0025: DynamoDB table encryption does not use a customer-managed KMS key.
# Using AWS managed key (aws/dynamodb) is acceptable for this project.
AVD-AWS-0025

# AWS-0066: Lambda X-Ray tracing is not enabled in Terraform.
# Tracing is managed by lambroll (app/function.jsonnet sets TracingConfig.Mode=Active).
AVD-AWS-0066
