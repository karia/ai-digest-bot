# AWS SDK — bundled in the Lambda Python runtime, do not ship our own copy
boto3/*
botocore/*

# Compiled bytecode (regenerated by the runtime)
*.pyc
*.pyo

# Type stubs (never imported at runtime)
*.pyi

# Bundled test suites (not imported at runtime)
*/tests/*
