# AWS SDK — bundled in the Lambda Python runtime, do not ship our own copy
boto3/*
botocore/*

# Package metadata (not needed at runtime)
*.dist-info*

# Compiled bytecode
*.pyc
*.pyo

# Type stubs (not needed at runtime)
*.pyi

# Test suites bundled inside dependencies
*/tests/*
